<h1>Categories</h1>
<ul>
  <% @categories.each do |category| %>
    <br>
    <li>
    <strong><%= link_to category.name, category_path(category) %></strong> (<%= pluralize(category.wines.count, "wine") %>)
    </li>
    
  <% end %>
</ul>

